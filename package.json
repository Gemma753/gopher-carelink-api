{
  "name": "gopher-carelink-api",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "jest --ci --coverage --reporters=default --reporters=jest-junit",
    "build": "mkdir build || echo Build folder exists && tar -a -c -f build\\gopher-carelink-api.zip package.json src"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "helmet": "^8.1.0"
  },
  "devDependencies": {
    "jest": "^30.2.0",
    "jest-junit": "^16.0.0",
    "snyk": "^1.1299.1",
    "supertest": "^7.1.4"
  },
  "jest-junit": {
  "outputDirectory": "reports/junit",
  "outputName": "junit.xml"
}

}
